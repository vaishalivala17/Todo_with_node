<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Todo</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="container mt-5">
    <div class="card p-4">
        <h2 class="title-glow">Edit Todo</h2>

        <form action="/update/<%= id %>" method="POST" class="mt-3">

            <label class="form-label">Todo Name</label>
            <input type="text" class="form-control mb-3" name="todos"
                   value="<%= todo.name %>" required>

            <label class="form-label">Description</label>
            <input type="text" class="form-control mb-3" name="description"
                   value="<%= todo.description %>" required>

            <label class="form-label">Task Status</label>
            <select class="form-select mb-3" name="task">
                <option value="incomplete" <%= todo.task === 'incomplete' ? 'selected' : '' %>>Incomplete</option>
                <option value="complete" <%= todo.task === 'complete' ? 'selected' : '' %>>Complete</option>
            </select>

            <label class="form-label">Priority</label>
            <select class="form-select mb-3" name="priority">
                <option value="high" <%= todo.priority === 'high' ? 'selected' : '' %>>High</option>
                <option value="medium" <%= todo.priority === 'medium' ? 'selected' : '' %>>Medium</option>
                <option value="low" <%= todo.priority === 'low' ? 'selected' : '' %>>Low</option>
            </select>

            <button type="submit" class="btn btn-custom w-100">Update</button>
        </form>

        <a href="/viewTodo" class="mt-3 d-block text-center">Back</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
